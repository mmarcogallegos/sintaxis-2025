program programaPrueba;

var
  radio:real;
  area:real;
  nombre: string; 
  mensaje: string;
  a:string;
end;

begin

  read("Ingrese su nombre: ", nombre);
  read("Ingrese el radio: ", radio);

  if radio > 0 and length(nombre) > 0 then 
   
    area := 3.1415 * (radio * radio);
    
    mensaje := "Calculo exitoso para " concat nombre;
    
    write("Resultado: ", mensaje, area);
  
  else
  
    write("Error: Valores invalidos");
  
  end;  

  while radio < 10 do
    radio := radio + 1;
  end;

  write(radio);

end.
